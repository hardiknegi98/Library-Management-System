# Library-Management-System

A backend system consisting of a monolithic spring boot service for proper management of college library.

*) Service exposes CRUD APIs for various entities in the system along with ordering and returning of books following a proper business logic.

*)  Followed Spring MVC design pattern for proper code structure along with proper db schema design methods and hibernate for scalability.
